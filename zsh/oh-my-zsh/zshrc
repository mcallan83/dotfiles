export DOTFILES=$HOME/.dotfiles
export ZSH=$DOTFILES/vendor/zsh/oh-my-zsh

DISABLE_AUTO_UPDATE=true
ZSH_CUSTOM=$DOTFILES/zsh/oh-my-zsh/custom

plugins=(
    ansible-custom
    brew
    composer-custom
    docker-custom
    extract
    git
    git-custom
    laravel-sail-custom
    laravel5
    sublime
    vagrant-custom
    vi-mode
    yarn
    z
    zsh-syntax-highlighting
)

PATH="/usr/local/sbin:$PATH:$DOTFILES/zsh/oh-my-zsh/bin"

source $ZSH/oh-my-zsh.sh

for file in $DOTFILES/zsh/oh-my-zsh/includes/*.zsh; do
    source "$file"
done

if [[ $(uname) == 'Darwin' ]]; then
    source $DOTFILES/zsh/oh-my-zsh/aliases-macos.zsh
    source $DOTFILES/zsh/oh-my-zsh/functions-macos.zsh
fi

